{
  "name": "@steadystart/config",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch --preserveWatchOutput --incremental",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "@steadystart/prisma": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
